/**
 *  Tests if the given value is an `arguments` object.
 *  @param v A value to test.
 *  @returns `true` if the value is an `arguments` object, `false` otherwise.
 */
export declare function isArguments(
    v : any
) : v is IArguments;
